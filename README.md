# Africa-Web3-Institute

## Development

This repository contains both the frontend (Vite/React) and backend (Express/SQLite) applications.

To run both concurrently, open a terminal in this root directory and run:

```bash
npm install
npm run dev
```

This will automatically start the backend on `http://localhost:3001` and the frontend on its respective port.
